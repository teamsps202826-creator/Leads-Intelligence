# The backend implements every endpoint the app uses, so real data is the
# default. Set this to true to run on the fixtures in src/api/mocks.js instead
# — useful for designing against states that are tedious to reproduce (a lead
# with no attributable email, a task mid-scan, an unprofiled product).
# VITE_USE_MOCKS=true
